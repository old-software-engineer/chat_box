var conversations = $('#chat-box-container');
conversations.append("<%= escape_javascript(render 'chat_box/conversations/group_modal',group: @group ,user_list: @users, current_user: current_user) %>");
conversations.find('#ChatBoxGroupModal').dialog({
	resizable: false,
	height: "auto",
	width: 400,
	modal: true
});
